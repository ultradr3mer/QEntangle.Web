@model ChoicesListViewModel

<div class="row limited">
  @foreach (var choice in Model.List)
  {
    <div class="column small-12">
      <h2>@choice.Name</h2>
      <h3>@choice.Options</h3>
      <h4>@choice.DefinitiveOption</h4>
    </div>
  }
</div>
